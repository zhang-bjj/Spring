server:
  port: 8010
spring:
  application:
    name: couldbjj-consumer-ribbon
  devtools:
    restart:
      enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true